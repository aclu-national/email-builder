{%- if config.environment === 'prod' -%}
{signature = 'ed'}{include "signatures"}
{%- else -%}
	<p><img src="{{dataLoader.nat.signatures.ed.headshot}}" alt="{{dataLoader.nat.signatures.ed.name}}" border="0" width="90" /></p>

	<p><strong>{{dataLoader.nat.signatures.ed.name}}<br />
	Pronouns: {{dataLoader.nat.signatures.ed.pronouns}}<br />
	{{dataLoader.nat.signatures.ed.title}}, {{dataLoader.nat.affiliation_name}}</strong></p>
{%- endif -%}

