<tr>
	<td align="left" style="padding:20px 0;" class="logo-spacing">
		<a href="{% if config.environment === 'prod' %}{website_url}{% else  %}{{dataLoader.nat.website_url}}{% endif %}"><img alt="{% if config.environment === 'prod' %}{affiliation_name}{% else  %}{{dataLoader.nat.affiliation_name}}{% endif %}'s logo" src="{% if config.environment === 'prod' %}{logo_url}{% else  %}{{dataLoader.nat.logo_url}}{% endif %}" border="0" width="{% if config.environment === 'prod' %}{logo_width}{% else %}{{dataLoader.nat.logo_width}}{% endif %}" style="display:block;"/></a>
	</td>
</tr>